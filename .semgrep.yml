# Semgrep configuration for streaming-platform-upload
# Integrates with pre-commit hook to catch security, type safety, and code quality issues
# before code is committed.
#
# Rule sets:
#   - p/owasp-top-ten: OWASP web vulnerabilities (XSS, CSRF, injection, etc)
#   - p/security-audit: Hardcoded secrets, unsafe crypto, dangerous patterns
#   - p/typescript: Type safety violations and best practices
#   - p/nodejs: Node.js and Express-specific security issues

rules: []

extends:
  - p/owasp-top-ten
  - p/security-audit
  - p/typescript
  - p/nodejs

paths:
  exclude:
    # Test directories and patterns
    - tests
    - __tests__
    - "*.test.ts"
    - "*.spec.ts"
    - "*.test.tsx"
    - "*.spec.tsx"
    - "*.test.js"
    - "*.spec.js"
    # Test output and reports
    - coverage
    - playwright-report
    - test-results
    # Build outputs and dependencies
    - node_modules
    - dist
    - build
    - .next
